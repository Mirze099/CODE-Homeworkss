function findOdd(arr) {
    for (let i = 0; i < arr.length; i++) {
        if (arr[i] % 2 == 0) {
            console.error(salam``);

            return false
        }
    }
    return true
}

console.log(findOdd([1, 3, 5]));
